import styled from "styled-components";

export const BoxPhotoComments = styled.ul`
  max-height: 300px;
  overflow-y: auto;
  word-break: break-word;
  padding: 0.2rem;

  li {
    margin-bottom: 0.5rem;
    line-height: 1.2;
  }
`;
